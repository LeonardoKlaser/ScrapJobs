name: 'Test Workflow'

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/test.yml' 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "Hello World! The runner is working."