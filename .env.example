# =============================================================================
# ScrapJobs — Variáveis de Ambiente
# Copie este arquivo para .env e preencha os valores antes de rodar localmente.
# NUNCA commite o arquivo .env com valores reais.
# =============================================================================

# -----------------------------------------------------------------------------
# Banco de Dados (PostgreSQL)
# -----------------------------------------------------------------------------
HOST_DB=localhost
PORT_DB=5432
USER_DB=scrapjobs
PASSWORD_DB=scrapjobs
DBNAME=scrapjobs

# -----------------------------------------------------------------------------
# Redis (usado pelo Asynq para filas de tarefas)
# -----------------------------------------------------------------------------
REDIS_ADDR=localhost:6379

# -----------------------------------------------------------------------------
# JWT (autenticação de usuários)
# -----------------------------------------------------------------------------
JWTTOKEN=seu_jwt_secret_aqui_minimo_32_chars

# -----------------------------------------------------------------------------
# Gemini AI (análise de currículo x vaga)
# -----------------------------------------------------------------------------
GEMINI_KEY=sua_gemini_api_key_aqui
AI_MODEL=gemini-1.5-flash

# -----------------------------------------------------------------------------
# AbacatePay (gateway de pagamento)
# -----------------------------------------------------------------------------
ABACATEPAY_API_KEY=sua_abacatepay_api_key_aqui
ABACATEPAY_BASE_URL=https://api.abacatepay.com
ABACATEPAY_WEBHOOK_SECRET=seu_webhook_secret_aqui
ABACATEPAY_PUBLIC_KEY=sua_public_key_abacatepay_aqui

# -----------------------------------------------------------------------------
# Frontend (usado para gerar URLs de redirecionamento pós-pagamento)
# -----------------------------------------------------------------------------
FRONTEND_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# AWS SES (envio de e-mails transacionais)
# Deixe em branco para desabilitar o envio de e-mails em desenvolvimento.
# -----------------------------------------------------------------------------
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
SES_SENDER_EMAIL=noreply@scrapjobs.com.br

# -----------------------------------------------------------------------------
# AWS S3 (upload de logos de empresas — opcional para MVP)
# Deixe S3_BUCKET_NAME em branco para desabilitar o upload de logos.
# -----------------------------------------------------------------------------
S3_BUCKET_NAME=

# -----------------------------------------------------------------------------
# AWS Secrets Manager (usado apenas em produção na AWS)
# Deixe em branco para usar as variáveis de ambiente acima diretamente.
# -----------------------------------------------------------------------------
APP_SECRET_NAME=

# -----------------------------------------------------------------------------
# Gin (modo de execução do servidor HTTP)
# Use "release" em produção para desabilitar logs de debug do Gin.
# -----------------------------------------------------------------------------
GIN_MODE=debug
